import { styled } from '@mui/material';
import { GridSearchIcon } from '@mui/x-data-grid';

export const SearchIcon = styled(GridSearchIcon)(() => ({
  color: 'rgb(255, 140, 68)',
  height: '30px',
  width: '30px',
}));
